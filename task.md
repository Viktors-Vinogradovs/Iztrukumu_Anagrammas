Rules:
1) We get a list of ALL 6 letter NOUNS (place names are also OK, such as Bauska) from vuordineica.lv
	1a. We get all the list, filter for all 6 letter entries that have normal noun endings (a, e, o, s, š, maybe i for preiļi etc). then we have a script that opens each of the possible noun entries and checks if it Is, indeed, a noun. If yes, save to our master data list.
	
2) we randomly select a word, then replace a random letter with ? and scramble it up (Bauska --> Ba?ska --> kaa?sb --> print this on the screen. We accept Bauska as a correct answer, IF the user has submitted, say, Kabans (replacing ? with n and creating a word) we check - if this word IS indeed in our master list, we also accept it.

3) as a variation, we start with B??ska, and accept Bauska and anything else from master list, that has letters B, s, k, a. If user can't think of a word, he clicks question mark and gets one extra letter (randomly), say, B?uska. Proceed as in step 2.

Why? Well, I am going to another quiz game too, and the dude there has Anagrammu raunds, where first letters from the answers to his questions make up an anagramm. And we usually are a few letters short :D

Once we have the master list (and i we did do something super similar to get word list for chemical app), the rest our artificial friends will create quickly.

4) The "not sure" version takes 'Bauska', removes one letter 'Bau?ka', select one more letter and say: it's either K or T (second letter - random, order displayed K or T, T or K random), so you get A U ? B [T/K] A.